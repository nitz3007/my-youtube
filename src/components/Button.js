const Button = ({name}) => {
    return <button className="px-4 py-1 bg-[#eeeeee] rounded-md my-3 mx-1 text-sm hover:bg-[#dddddd] font-semibold">
        {name}
    </button>
}

export default Button;